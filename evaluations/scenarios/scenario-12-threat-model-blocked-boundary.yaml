schema_version: "1.0"
scenario_id: scenario-12-threat-model-blocked-boundary
title: "脅威モデル評価: 信頼境界が一致しない候補をblockedにする"
scenario_type: malware
status: threat_model_fixture
asset:
  name: scenario-12-threat-model-blocked-boundary
  type: endpoint
  privilege_level: user
flows:
  - from: internet
    to: scenario-12-threat-model-blocked-boundary
    protocol: https
    trust_boundary: internet-to-dmz
    authentication:
      required: false
    authorization:
      required: false
entrypoint:
  type: malware_behavior
  source: safe_synthetic_threat_model_fixture
  identifiers:
    behavior: abstract_process_execution
    attack_techniques: [T1059]
analysis_chain:
  weakness: 実際のマルウェアではなく、信頼境界条件の不一致を評価する。
  exploitation: 実行手順やペイロードは含めず、ATT&CK Techniqueだけを候補にする。
  attacker_action: T1059を候補として評価する。
  monitoring: Process Creationの抽象イベントを参照する。
  required_trust_boundary: dmz-to-internal
  required_privilege: user
  required_logs:
    - event_type: process_creation
      fields: [event_id, process.command_line]
available_logs: [process_creation]
expected_outcome:
  attack_applicability: blocked
  blocked_reason: trust_boundary
positive_samples: ../samples/multidomain/scenario-12-positive.jsonl
negative_samples: ../samples/multidomain/scenario-12-negative.jsonl
comparison:
  baseline: no_candidate_rule
  treatment: generated_detection_candidate
  metrics: [tp, fp, fn, tn, precision, recall, f1]
  status: not_measured
evidence:
  fixture_backed_ids: [T1059, DET0001]
  safety: 境界ラベルの比較だけを行い、実ネットワークは使用しない。
