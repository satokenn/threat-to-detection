schema_version: "1.0"
scenario_id: scenario-06-identity-credential-access
title: 認証情報アクセスを入口に必要ログを分析する
scenario_type: identity
status: not_evaluated
entrypoint:
  type: identity_behavior
  source: public_behavior_knowledge
  identifiers:
    behavior: abstract_credential_access
    attack_techniques: []
analysis_chain:
  weakness: CVEを起点にせず、認証情報への不審なアクセスを分析対象にする。
  exploitation: 実アカウント、資格情報、攻撃手順を用いず抽象イベントで表現する。
  attacker_action: Techniqueはfixtureに裏付けがないため確定しない。
  monitoring: Authentication、Identity、Processの相関が必要になる可能性を記録する。
  required_logs:
    - event_type: identity.authentication
      fields: [timestamp, event_id, user, outcome]
available_logs: [identity_authentication]
expected_gaps:
  - stage: behavior-to-ATT&CK
    reason: 現行fixtureに認証・ID分野のTechniqueがない。
  - stage: Technique-to-Detection-Requirement
    reason: Technique未確定のため検知候補を生成しない。
positive_samples: ../samples/multidomain/scenario-06-positive.jsonl
negative_samples: ../samples/multidomain/scenario-06-negative.jsonl
comparison:
  baseline: no_candidate_rule
  treatment: generated_detection_candidate
  metrics: [tp, fp, fn, tn, precision, recall, f1]
  status: not_measured
evidence:
  fixture_backed_ids: []
  result_policy: IDを推測で補わず、追加snapshotまたは明示的な出典が揃うまで未評価とする。
